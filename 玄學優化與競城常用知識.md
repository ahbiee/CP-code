# 基本Fast IO

```cpp
ios::sync_with_stdio(0);
cin.tie(0);
```

# 暴力解精簡優化

```cpp
#pragma GCC optimize("O3,unroll-loops") // 極致壓縮就把 O3 換成 Ofast
#pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt")
```

# define

```cpp
#define int long long // 在發現數值會超出int時可將 int 改為 long long，需記得將main改成 int32_t 或 signed
#define pb(x) push_back(x)
#define eb(x) emplace_back(x)
#define mp(x, y) make_pair(x, y)

using ll = long long;
using pii = pair<int, int>;
```

# Linux 環境下的編譯執行

```bash
g++ solve.cpp -o main -std=c++14 -fsanitize=undefined -Wall -Wextra -Wshadow

# 可以使用 alias 把原本要打的那串 define 掉 ， alias [name]='[value]'

alias g++ = `g++ -std=c++14 -fsanitize=undefined -Wall -Wextra -Wshadow`
```

# 用 python 產生隨機測資

```py
# 功能
from random import *
 
n = randint(1, 100)                       # 隨機產生 1~100的整數
ch = chr(ord('a') + randint(0, 25))       # 隨機產生 'a'~'z' 其中一個字元
choiceSet = sample(s, 4)                  # 從集合 s 選出 4 個不同的元素
choiceSet = sample(range(1, n+1), 4)      # 從整數 1~n 選出 4 個不同的元素
shuffle(arr)                              # 把序列 arr 順序打亂
```
```py
# 產生一組 1~n 的 permutation
from random import *
 
n = randint(3, 6)
 
arr = [i+1 for i in range(n)]
shuffle(arr)
 
print(n)
print(*arr)    #輸出 arr 的內容並且用空格隔開
```
```py
# 產生一個長度為 n 的小寫字母字串
	s = ""
n = randint(1, 10)
 
for i in range(n):
    s += chr(ord('a') + randint(0, 25))
print(s)
```
```py
# 產生一棵樹
from random import *
 
n = randint(3, 6)
 
print(n)
for i in range(2, n+1):
    print(randint(1, i-1), i)
```
```py
# 產生無自環無重邊的一張連通圖
from random import *
 
n = randint(5, 10)
m = randint(n-1, n+3)
 
print(n, m)
 
edge = list()
 
#construct tree
for i in range(2, n+1):
    x = randint(1, i-1)
    y = i
    edge.append([min(x, y), max(x, y)])
    print(x, y)
 
#add extra edge
for i in range(m-(n-1)):
    x = randint(1, n)
    y = randint(1, n)
    while x == y or [min(x, y), max(x, y)] in edge:
        x = randint(1, n)
        y = randint(1, n)
    print(x, y)
    edge.append([min(x, y), max(x, y)])
```